import styled from '@emotion/styled'

import { asideMarginX } from '../main-content.styled'

export const Root = styled.aside`
  min-width: 240px;
  width: 240px;
  margin-right: ${asideMarginX}px;
  padding-left: 6px;
`

export const Title = styled.h4`
  display: inline-block;
  margin-bottom: 12px;
  font-weight: 600;
`
