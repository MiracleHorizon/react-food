import styled from '@emotion/styled'

export const Root = styled.div`
  position: absolute;
  left: 0;
  top: calc(100% + 7px);
  width: 100%;
  max-height: 530px;
  padding: 4px 20px;
  border-radius: 16px;
  background: white;
  box-shadow: 0 2px 7px 0 rgba(0, 0, 0, 0.15);
  overflow: auto;
  z-index: 100;
`
