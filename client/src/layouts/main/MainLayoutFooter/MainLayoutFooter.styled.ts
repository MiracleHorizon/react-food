import styled from '@emotion/styled'

import { colors } from '@styles/colors'

export const Footer = styled.footer`
  min-height: 280px;
  width: 100%;
  border-top: 1px solid ${colors.gray['6']};
  background: ${colors.gray['1']};
`
