import styled from '@emotion/styled'

const Price = styled.p`
  width: 120px;
  display: block;
  text-align: right;
  font-weight: 500;
  font-size: 24px;

  span {
    margin-left: 3px;
  }
`

export default Price
