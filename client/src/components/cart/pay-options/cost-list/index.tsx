import { observer } from 'mobx-react-lite'

const CostList = () => (
  <ul>
    {/*{OrderStore.costList.map(item => (*/}
    {/*  <CostItem key={item.title} {...item} />*/}
    {/*))}*/}
  </ul>
)

export default observer(CostList)
