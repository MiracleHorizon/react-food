import styled from '@emotion/styled'

// const otherElsTotalWidth = 55 + 55 + 240 + 32 + 32 + 320

const StyledWrapper = styled.main`
  width: calc((172px + 8px) * 4);
`

export default StyledWrapper
