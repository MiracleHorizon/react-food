import styled from '@emotion/styled'

import { DEFAULT_LAYOUT_ASIDE_PANEL_X_MARGIN } from '../DefaultLayoutContent.styles'

export const Root = styled.aside`
  width: 240px;
  padding-left: 6px;
  margin-right: ${DEFAULT_LAYOUT_ASIDE_PANEL_X_MARGIN}px;
`

export const Title = styled.h4`
  margin-bottom: 12px;
  font-weight: 600;
`
